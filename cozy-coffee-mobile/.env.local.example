# ============================================================
# 本地/临时运行 override 模板（已 gitignore，不入库）
# 用法：复制为 .env.local 后按需填值（.env.local 优先级高于 .env.development）
#   cp .env.local.example .env.local
# 场景示例：
#   A) 日常本地开发连本地后端     → 注释即可（默认走 .env.development 的 127.0.0.1）
#   B) 临时连服务器 http://IP 验证 → 取消下面两行注释并改 IP
# ============================================================

# 后端 API 基址（小程序 uni.request 前缀，一般到 /api 为止）
# VITE_API_BASE_URL=http://120.27.213.125/api

# 图片基址（Nginx /media → MinIO）
# VITE_IMAGE_BASE_URL=http://120.27.213.125/media/cozycoffee

# 注意：真实公网 IP / 密钥不要提交进 .env.production / .env.local，
#       只在本机 .env.local 或构建/CI 时注入。
