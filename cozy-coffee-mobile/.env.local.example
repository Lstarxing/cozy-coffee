# ============================================================
# 本地/临时运行 override 模板（已 gitignore，不入库）
# ============================================================
# 【重要·实测结论】uni-app（Vite）读取 env 的优先级：
#     .env.[mode].local  >  .env.[mode]  >  .env.local  >  .env
#   即：.env.local 会被 .env.production / .env.development 「压过」。
#   mp-weixin 构建实测：只有 .env.production 时 .env.local 完全不生效（仍读 127.0.0.1）。
#   → 想临时连服务器，请用【模式级 local 文件】，不要用 .env.local。
#
# 用法：
#   构建路线（dist/build/mp-weixin）：新建 .env.production.local
#   热更路线（dist/dev/mp-weixin）  ：新建 .env.development.local
#   两者都已 gitignore（.gitignore 的 .env.*.local），公网 IP 不会入库。
# ============================================================

# 后端 API 基址（小程序 uni.request 前缀，一般到 /api 为止）
# VITE_API_BASE_URL=http://<服务器IP>/api

# 图片基址（Nginx /media → MinIO）
# VITE_IMAGE_BASE_URL=http://<服务器IP>/media/cozycoffee

# 注意：真实公网 IP / 密钥不要提交进入库的 .env.production / .env.development，
#       只写进 .env.[mode].local（本机，gitignore）。
